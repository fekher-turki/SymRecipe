{% extends 'base.html.twig' %}

{% block title %}
	SymRecipe - Accueil
{% endblock %}

{% block body %}
	<div class="p-5 mb-4 bg-body-tertiary rounded-3">
		<div class="container-fluid py-5">
			<h1 class="display-5 fw-bold">Bienvenue sur SymRecipe</h1>
			<p class="col-md-8 fs-4">SymRecipe est une application qui va te permettre de créer des recettes à base d'ingrédients que tu auras toi-même créer.</p>
			<p class="col-md-8 fs-4">Pour commencer, rendez-vous sur la page d'inscription pour utiliser l'application.</p>
			<a class="btn btn-primary btn-lg" href="#" role="button">Inscription</a>
		</div>

		<div class="recipes">
			<h2 class="mt-4">Recettes de la communauté</h2>
			<div class="class d-flex flex-wrap justify-content-between">
				{% for recipe in recipes %}
					<div class="card border-primary mb-3 me-3" style="max-width: 20rem;">
						<div class="card-header">Recette n°{{ recipe.id }}</div>
						<div class="card-body">
							<h4 class="card-title">{{ recipe.name }}</h4>
							<p class="card-text">{{ recipe.description|slice(0, 100) ~ '...' }}</p>
						</div>
					</div>
				{% endfor %}
			</div>
		</div>
	</div>
{% endblock %}
